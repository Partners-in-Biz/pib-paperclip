export const SEO_SPRINT_SKILL = `# SEO sprint

Use the \`partnersinbiz.seo\` tools to record rank and audit findings on a sprint.

- A sprint belongs to one site.
- Keywords, pages, and audits are rows on that sprint.
- \`add-keyword\` and \`add-page\` track what to watch. \`record-rank\` stores a keyword rank and snapshots it to the rank history.
- \`rank-history\` returns a keyword's rank over time. \`audit-summary\` groups findings by severity.
- When a person must do the work, call \`open-task\`. That opens a Paperclip issue tagged with the sprint id.
- Do not create a second board for sprint tasks.
`;
